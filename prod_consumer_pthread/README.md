# Producer - Consumer Algorithm Implementation using PThreads

This project demonstrates the implementation of producer consumer pthreads capable of running on multi-core.

### Requirements:
* Linux host to build
* PThreads library
* build-essential package

### Build
run `$ make`
